// complete the code below to make it compile and succeed

fn main() { // change nothing here
    print_text();
    println!("success!");
}

// create a function "print_text" that prints "Hello, world!" to the console
// functions do not need to have an input or output, but they do still need to be declared
// fn function_name(input: type) -> return_type { // this is the syntax for declaring a function

??? print_text(???) ??? {
    ???
}